# Search Parameter Coverage Matrix

This matrix tracks search-facing parameter coverage at a finer level than route
parity. The goal is to answer a narrower question:

- for a given query, ranking, aggregation, suggest, highlight, or vector
  parameter family,
- is Steelsearch implemented,
- is it covered by strict compare,
- is it covered by semantic probes,
- does unsupported behavior fail closed,
- and what is still missing before replacement claims are safe.

## Column Definitions

| Column | Meaning |
| --- | --- |
| `Family` | Parameter or DSL family being tracked. |
| `Surface` | Route family where the parameter matters, such as `/_search`, `/{index}/_search`, `_msearch`, or template-search surfaces. |
| `Implemented` | `yes`, `partial`, or `no` based on the current runtime behavior. |
| `Strict compared` | Whether the family is represented in `search-strict-compat.json` and expected to match canonicalized OpenSearch output closely. |
| `Semantic probed` | Whether the family is pinned by a lighter semantic probe or compare fixture even when strict parity is not claimed. |
| `Fail closed` | Whether unsupported or incomplete variants return an explicit failure rather than being silently ignored. |
| `Evidence` | Primary fixture, code, or artifact that currently backs the claim. |
| `Code path / missing path` | The current handler/evaluator location in `standalone_runtime.rs`, or an explicit note that no dedicated handler/evaluator exists yet. |
| `Notes / missing work` | Why the family is still partial and what remains to close it. |

## Query-String Parameters

| Family | Surface | Implemented | Strict compared | Semantic probed | Fail closed | Evidence | Code path / missing path | Notes / missing work |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `from` / `size` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json` | `handle_index_search_route` at [standalone_runtime.rs:5667](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5667) applies pagination after query evaluation. | Semantic smoke now pins sorted windowing; unsupported pagination combinations still need negative expansion. |
| `sort` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/search-sort-unknown-option-compare-current/search-compat-report.json` | `apply_search_sort` at [standalone_runtime.rs:13296](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:13296); request validation in [standalone_runtime.rs:12283](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12283). | Sorted window coverage exists in both strict and semantic suites; unknown field-sort options are now live-compared against OpenSearch via `sort_unknown_field_option_error_search` and fail closed instead of being silently ignored. Broader sort-family combinations remain partial. |
| `track_total_hits` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json` | handled in request validation and response shaping at [standalone_runtime.rs:12283](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12283) and [standalone_runtime.rs:5869](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5869). | `true` and threshold forms are now both pinned, but route/profile matrix expansion is still pending. |
| `allow_partial_search_results` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json`, `crates/os-node/src/standalone_runtime.rs` | query-param boolean validation and bounded partial-shard failure enforcement in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs). | `allow_partial_search_results_true_search` pins OpenSearch response parity for the accepted true path; `false` also turns the current modeled partial shard failure subset into a search-phase failure, while broader shard failure families still need matrix expansion. |
| `pre_filter_shard_size` / `batched_reduce_size` / `max_concurrent_shard_requests` | `/_search`, `/{index}/_search` | partial | yes | yes | yes | `tools/fixtures/search-strict-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json`, `crates/os-node/src/standalone_runtime.rs` | query-param integer parsing and OpenSearch lower-bound validation in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs). | `search_coordinator_knobs_search` pins accepted request-shape parity for all three coordinator knobs; `pre_filter_shard_size_lower_bound_error_search`, `batched_reduce_size_lower_bound_error_search`, and `max_concurrent_shard_requests_lower_bound_error_search` strict-compare lower-bound rejection. Steelsearch does not model distributed reduce concurrency yet. |
| `phase_took` / `verbose_pipeline` / `track_scores` / `include_named_queries_score` | `/_search`, `/{index}/_search` | partial | yes | yes | yes | `tools/fixtures/search-strict-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json`, `crates/os-node/src/standalone_runtime.rs` | query-param boolean parsing in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs); `track_scores` also controls field-sort score rendering. | `track_scores_field_sort_search` pins OpenSearch parity for score rendering under field sort, `search_boolean_execution_query_params` pins the accepted `phase_took` / `verbose_pipeline` / `track_scores` / `include_named_queries_score` query-param bundle, and invalid boolean query-param rejection is strict-compared for each field. Named-query score detail remains bounded request-shape coverage. |
| `q` / `df` / `default_operator` URL query string | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json`, `crates/os-node/src/standalone_runtime.rs` | URL `q` is converted to a bounded `query_string` query before search validation/evaluation in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs). | `query_string_url_q_overrides_body_search` pins that `q` overrides body `query` like OpenSearch and honors `df` plus `default_operator`; analyzer, wildcard analysis, and lenient URL variants fail closed until analyzer-equivalent semantics are implemented. |
| `allow_no_indices` | `/_search`, `/{index}/_search`, `_count` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json` | search target resolution in [standalone_runtime.rs:3135](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:3135) and [standalone_runtime.rs:12034](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12034); `_count` path is bounded in `handle_count_route` at [standalone_runtime.rs:5350](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5350). | Search empty-wildcard coverage exists in strict and semantic suites, and invalid search/count-route boolean values are strict-compared against OpenSearch. `_count` also has semantic evidence for empty wildcard and `allow_no_indices=true`, both returning bounded `200 + count=0`; broader target-resolution combinations remain partial. |
| `ignore_unavailable` | `/_search`, `/{index}/_search`, `_count` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json` | target resolution in [standalone_runtime.rs:3135](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:3135) and [standalone_runtime.rs:12034](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12034). | Multi-target missing-index behavior and invalid search/count-route boolean values are now pinned in strict comparisons; broader target-resolution combinations remain partial. |
| `q` query-string search | `_count` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json`, `crates/os-node/src/standalone_runtime.rs` | `handle_count_route` uses the shared bounded URL `q` to `query_string` conversion in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs). | `count_query_string_url_q_search` pins `_count?q=...` against OpenSearch for bounded `field:value` query-string cases; analyzer-equivalent URL variants still fail closed. |
| `rewrite` / explain-style validate options | `/_validate/query`, `/{index}/_validate/query` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json`, `crates/os-node/src/standalone_runtime.rs` | `handle_validate_query_route` at [standalone_runtime.rs:5470](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5470) now rejects unsupported rewrite shapes via `build_unsupported_search_response`; no dedicated handling exists for any broader explain-style validate option set. | `validate_query_target_rewrite_search` pins the bounded `rewrite=true` term-query explanation against OpenSearch, and `validate_query_empty_search` pins empty-query invalid behavior. Broader explain-style validate options remain partial. |

## Body Query DSL Families

| Family | Surface | Implemented | Strict compared | Semantic probed | Fail closed | Evidence | Code path / missing path | Notes / missing work |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `match_all` | `/_search`, `/{index}/_search`, `_count` | yes | partial | yes | n/a | `tools/fixtures/search-semantic-compat.json`, `crates/os-node/src/standalone_runtime.rs` | `matches_query_body` at [standalone_runtime.rs:15822](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:15822); count path in [standalone_runtime.rs:5329](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5329). | Basic search/count semantics are pinned. `_count` now also pins current bounded policy that both empty wildcard targets and missing exact indices return `200 + count=0`. |
| `term` | `/_search`, `/{index}/_search`, `_count`, `_explain` | yes | yes | yes | n/a | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json` | `matches_query_body` at [standalone_runtime.rs:15822](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:15822); explain path in [standalone_runtime.rs:5496](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5496). | One of the strongest covered lexical families. |
| `match` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/search-match-unknown-option-compare-current/search-compat-report.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | `matches_query_body` at [standalone_runtime.rs:15822](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:15822). | `match_query_scoring_order_total_hits` pins a bounded indexed-field `match` hit/scoring-order case against OpenSearch; `match_operator_and_search` pins `operator=and` all-term semantics; `match_fuzziness_search`, `match_fuzziness_prefix_length_search`, and `match_fuzzy_transpositions_search` now pin bounded edit-distance semantics including `prefix_length` and `fuzzy_transpositions`; `match_zero_terms_query_all_search` pins empty analyzed input with `zero_terms_query=all`; `match_unknown_option_error_search` pins OpenSearch-style rejection of unknown match options instead of silently ignoring them. Broader match-option semantics remain partial. |
| `multi_match` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/search-multi-match-option-compare-current/search-compat-report.json` | evaluated through `matches_query_body` / search path at [standalone_runtime.rs:29518](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:29518). | Best-fields semantic coverage is pinned; `multi_match_single_field_string_search`, `multi_match_boosted_field_string_search`, `multi_match_phrase_keyword_analyzer_search`, `multi_match_phrase_prefix_keyword_analyzer_search`, `multi_match_fuzziness_search`, `multi_match_fuzziness_prefix_length_search`, `multi_match_fuzzy_transpositions_search`, `multi_match_zero_terms_query_all_search`, and `multi_match_unknown_option_error_search` now pin OpenSearch-style `fields` string parsing, field boost base-name resolution, phrase keyword-analyzer semantics, phrase-prefix non-text field rejection, bounded edit-distance semantics including `prefix_length` and `fuzzy_transpositions`, empty analyzed input with `zero_terms_query=all`, and unknown option rejection. Broader multi-match type/scoring semantics remain partial. |
| `query_string` / `simple_query_string` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `target/query-string-options-current/opensearch-compare-report.json` | standalone evaluation in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs), DSL parsing in [os-query-dsl/lib.rs](/home/ubuntu/steelsearch/crates/os-query-dsl/src/lib.rs), and native tokenized field-set query build in [os-engine-tantivy/lib.rs](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs). | `query_string_search`, `query_string_minimum_should_match_search`, `simple_query_string_search`, and `simple_query_string_minimum_should_match_search` pin bounded text-query semantics against OpenSearch; DSL/native paths now preserve `default_operator` and `minimum_should_match`. Broader parser syntax, analyzer, escaping, wildcard analysis, and scoring parity remain partial. |
| `match_phrase` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/search-phrase-option-compare-current/search-compat-report.json` | lexical evaluation in [standalone_runtime.rs:29494](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:29494). | Core phrase path is covered by strict and semantic fixtures; `match_phrase_object_query_search`, `match_phrase_keyword_analyzer_search`, `match_phrase_zero_terms_query_all_search`, and `match_phrase_unknown_option_error_search` pin object-form `query` extraction, keyword analyzer semantics, empty analyzed input with `zero_terms_query=all`, and OpenSearch-style unknown option rejection. Broader phrase option semantics remain partial. |
| `match_phrase_prefix` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/search-phrase-option-compare-current/search-compat-report.json`, `target/rest-compat-current-3/search-compat-report.json` | lexical evaluation in [standalone_runtime.rs:29503](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:29503). | Prefix phrase semantic coverage is pinned; `match_phrase_prefix_object_query_search`, `match_phrase_prefix_keyword_analyzer_search`, `match_phrase_prefix_max_expansions_search`, `match_phrase_prefix_slop_search`, `match_phrase_prefix_zero_terms_query_all_search`, and `match_phrase_prefix_unknown_option_error_search` pin object-form `query`, keyword analyzer semantics, bounded `max_expansions` admission, `slop` semantics, empty analyzed input with `zero_terms_query=all`, and OpenSearch-style unknown option rejection. Broader prefix phrase option semantics remain partial. |
| `bool` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `crates/os-node/src/standalone_runtime.rs`, `crates/os-engine-tantivy/src/lib.rs` | lexical evaluation in [standalone_runtime.rs:15822](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:15822). | Filter/range combination is covered in strict fixtures; `bool_object_form_array_keyword_filter_search` now pins object-form `must`/`filter`/`must_not` clauses with array keyword term filtering against OpenSearch; `bool_percentage_minimum_should_match_search` and `bool_conditional_minimum_should_match_search` now pin percentage-form and conditional-form `minimum_should_match` against OpenSearch; nearby semantic evidence now also pins bounded `must_not`-only and zero-`minimum_should_match` behavior, route-level runtime tests now pin the same bounded bool semantics directly, and nearby native-engine regression coverage now also pins empty-bool match-all, pure-negative bool, and optional-`should`-only zero-`minimum_should_match` not only in candidate semantics but also in representative native hit/count surfaces, while the current lexical `must_not` bool seat no longer preemptively requires native document scan solely because of that exclusion placement, helper-level native hit-context and reusable-query-context assembly now also prefer native-buildable query paths before falling back to document-backed bool assembly, and the native count seat keeps the direct `Count` collector seam, with nearby regression coverage now directly pinning both pure-negative and mixed-positive lexical `must_not` count examples, pure-negative and mixed-positive lexical `must_not` native hit-context examples, pure-negative and mixed-positive lexical `must_not` native document examples, a mixed-positive lexical `must_not` native hit example, pure-negative plus mixed-positive lexical `must_not` reusable-context examples, and both pure-negative and mixed-positive reusable-context cases that also keep the optional-all-documents carrier; together with the current lexical native-document/hit/page/window/count and `size=0` aggregation fallback for `minimum_should_match > 1` bool queries, including representative hit-materialization-sensitive `top_hits`, nested-bool hit/page/window including explicit sort, nested-bool `size=0` aggregation, and nested-bool `top_hits` aggregation cases, the broader bool matrix is still incomplete. |
| `range` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `crates/os-engine-tantivy/src/lib.rs` | lexical evaluation in [standalone_runtime.rs:15822](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:15822); native keyword range is covered in [lib.rs](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs). | Semantic matrix now pins standalone keyword range over root search, and native regression tests pin single-index plus multi-index keyword range; broader range field families remain partial. |
| `exists` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json`, `crates/os-node/src/standalone_runtime.rs` | request-shape validation at [standalone_runtime.rs:15263](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:15263); lexical evaluation in [standalone_runtime.rs:16127](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:16127); targeted runtime coverage in [standalone_runtime.rs:24882](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:24882). | `exists_query_search` pins a positive present-field hit set against OpenSearch. Semantic evidence also covers missing/null non-match behavior and an explicit unsupported-shape `400`; broader parity questions remain semantic-audit follow-on work. |
| `dis_max` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/search-dis-max-option-compare-current/search-compat-report.json` | lexical evaluation in [standalone_runtime.rs:29636](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:29636). | Semantic matrix pins a bounded hit-set case with explicit `_id` sort; `dis_max_tie_breaker_search` and `dis_max_unknown_option_error_search` now pin OpenSearch-style `tie_breaker` admission and unknown option rejection. Broader tie-breaker score-order semantics remain partial. |
| `ids` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/search-ids-option-compare-current/search-compat-report.json` | lexical evaluation in [standalone_runtime.rs:29701](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:29701). | Positive semantic path is pinned; `ids_query_boost_search` and `ids_unknown_field_error_search` now pin OpenSearch-style standard-field admission and unknown field rejection. Broader failure-path and wildcard interactions remain incomplete. |
| `prefix` / `wildcard` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json` | lexical evaluation in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs), native query execution in [os-engine-tantivy/lib.rs](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs). | `prefix_query_search` and `wildcard_query_search` pin bounded keyword matching; `prefix_case_insensitive_search` and `wildcard_case_insensitive_search` now pin OpenSearch-style `case_insensitive` admission and matching for keyword fields. Broader rewrite, automaton, text-analysis, and scoring behavior remains partial. |
| `regexp` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json` | bounded lexical evaluation in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs), native query execution in [os-engine-tantivy/lib.rs](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs). | `regexp_query_search` pins bounded keyword regexp matching; `regexp_case_insensitive_search` now pins OpenSearch-style `case_insensitive` admission and matching for keyword fields. Broader automaton syntax, rewrite behavior, text-analysis, and scoring behavior remains partial. |
| `fuzzy` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json` | bounded lexical evaluation in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs), native query execution in [os-engine-tantivy/lib.rs](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs). | `fuzzy_query_search` pins bounded edit-distance matching; `fuzzy_query_prefix_length_search` and `fuzzy_query_prefix_length_miss_search` pin OpenSearch-style `prefix_length` matching/rejection; `fuzzy_query_transpositions_search` and `fuzzy_query_transpositions_disabled_search` pin adjacent-transposition behavior. Broader fuzziness modes, rewrite behavior, scoring, and analyzer interactions remain partial. |
| `terms_set` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/search-terms-set-option-compare-current/search-compat-report.json` | lexical evaluation in [standalone_runtime.rs:29789](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:29789). | Script-based `minimum_should_match_script` execution is pinned by `terms_set_query_search`; `terms_set_minimum_should_match_field_search` now pins document-field threshold evaluation; `terms_set_unknown_field_error_search`, `terms_set_minimum_should_match_error_search`, `terms_set_conflicting_minimum_sources_error_search`, and `terms_set_boost_search` pin OpenSearch-style option admission/rejection. Broader missing-field and multi-value threshold edge cases remain partial. |
| `function_score` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | bounded validation at [standalone_runtime.rs:21608](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:21608); source scoring at [standalone_runtime.rs:23011](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:23011). | `function_score_search` pins the supported inner-query + positive weight + `boost_mode=replace` path against OpenSearch; broader functions and score-order parity remain partial. |
| `script_score` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | bounded validation at [standalone_runtime.rs:21637](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:21637); source scoring at [standalone_runtime.rs:23030](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:23030). | `script_score_search` pins the supported numeric constant `script.source` path against OpenSearch; broader script semantics and fail-closed coverage remain incomplete. |
| invalid/unsupported query type | `/_validate/query`, `/_search`, `_count` | partial | partial | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/search-unknown-query-compare-current/search-compat-report.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | `_validate/query` handler at [standalone_runtime.rs:5448](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5448); `_count` validation in [standalone_runtime.rs:5350](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5350); unsupported search validation at [standalone_runtime.rs:12283](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12283). | `validate_query_empty_search` is now strict-compared. Root and targeted `_search` unknown query type failures are now live-compared against OpenSearch via `unknown_query_type_root_search_error` and `unknown_query_type_target_search_error`, including `parsing_exception` / `unknown query [...]` root-cause semantics. `_count` fail-closed unsupported query types and malformed payloads remain semantic evidence. |

## Ranking And Pagination Families

| Family | Surface | Implemented | Strict compared | Semantic probed | Fail closed | Evidence | Code path / missing path | Notes / missing work |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| sorted result window | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/search-sort-unknown-option-compare-current/search-compat-report.json` | `apply_search_sort` at [standalone_runtime.rs:13296](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:13296). | Happy-path sort window exists in strict and semantic fixtures; unknown field-sort options now have explicit OpenSearch-compared fail-closed policy. Broader sort combinations remain partial. |
| `search_after` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | `apply_search_after` at [standalone_runtime.rs:13343](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:13343); validation at [standalone_runtime.rs:12307](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12307). | `search_after_search` pins sorted date windowing against OpenSearch; semantic evidence also covers sorted keyword windowing and OpenSearch-shaped `from` validation failure, while broader sort-type variants remain partial. |
| `collapse` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | `apply_search_collapse` at [standalone_runtime.rs:13497](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:13497). | `collapse_search` pins keyword collapse against OpenSearch, and semantic evidence also covers OpenSearch-shaped `search_after` sort-field mismatch failure; inner hits and broader field-type behavior remain partial. |
| `rescore` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | `apply_search_rescore` at [standalone_runtime.rs:13448](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:13448); request validation at [standalone_runtime.rs:12321](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12321). | `rescore_search` pins the supported rescore path against OpenSearch; broader rescore variants remain partial. |
| `profile` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | no dedicated profiling subsystem; current support is bounded search response shaping at [standalone_runtime.rs:9536](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:9536) and [standalone_runtime.rs:18941](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:18941). | `profile_search` pins `profile: true` structural response presence against OpenSearch, and semantic evidence covers `profile: false` omission; unsupported-option audit remains partial. |
| `rank_eval` | `/_rank_eval`, `/{index}/_rank_eval` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json`, stateful probe + runtime tests | `handle_rank_eval_route` supports the strict-compared Precision@K subset; broader metrics/templates remain outside the current bounded implementation. | `rank_eval_precision_search` now pins OpenSearch-parity metric score, request details, precision detail, and unrated-doc handling. |

## Highlight And Suggest Families

| Family | Surface | Implemented | Strict compared | Semantic probed | Fail closed | Evidence | Code path / missing path | Notes / missing work |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `highlight` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | request validation at [standalone_runtime.rs:20986](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:20986); rendering at [standalone_runtime.rs:23248](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:23248). | `highlight_search` pins bounded highlight rendering against OpenSearch; semantic evidence also covers encoder admission and custom-tag match highlighting, while broader highlighter options and negative coverage remain partial. |
| `term suggest` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | request validation at [standalone_runtime.rs:21056](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:21056); response builder at [standalone_runtime.rs:23411](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:23411). | `term_suggest_search` pins bounded term suggest behavior against OpenSearch; broader spellchecker options remain partial. |
| `phrase suggest` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | request validation at [standalone_runtime.rs:21056](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:21056); response builder at [standalone_runtime.rs:23411](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:23411). | `phrase_suggest_search` pins OpenSearch-compatible empty bounded phrase options for the default field setup; broader phrase suggester options remain partial. |
| `completion suggest` | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | request validation at [standalone_runtime.rs:21056](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:21056); response builder at [standalone_runtime.rs:23411](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:23411). | `completion_suggest_search` pins bounded completion prefix + size behavior against OpenSearch; broader completion suggester options remain partial. |

## Aggregation Families

| Family | Surface | Implemented | Strict compared | Semantic probed | Fail closed | Evidence | Code path / missing path | Notes / missing work |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `adjacency_matrix` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native collection around [os-engine-tantivy/lib.rs:162226](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs:162226); standalone fallback at [standalone_runtime.rs:35950](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:35950). | `adjacency_matrix_aggregation` pins bounded two-filter term-query bucket and intersection counts against OpenSearch, and `adjacency_matrix_separator_intersecting_aggregation` pins custom `separator` plus `show_only_intersecting` bucket output; broader query families, keyed variants, nested aggregations, and high-cardinality filter sets remain partial. |
| `auto_date_histogram` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native collection around [os-engine-tantivy/lib.rs:161593](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs:161593); standalone fallback at [standalone_runtime.rs:36154](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:36154). | `auto_date_histogram_aggregation` pins bounded day-minimum bucket interval and date bucket output against OpenSearch, while `auto_date_histogram_format_aggregation`, `auto_date_histogram_time_zone_aggregation`, and `auto_date_histogram_missing_aggregation` pin bounded `format`, fixed-offset `time_zone`, and string date missing replacement; broader interval selection, IANA/DST time zones, broader format variants, and multi-shard rounding cases remain partial. |
| `date_histogram` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native collection in [os-engine-tantivy/lib.rs](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs); standalone fallback in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs). | `date_histogram_aggregation` pins bounded day buckets against OpenSearch, `date_histogram_keyed_aggregation` pins keyed object bucket output, `date_histogram_offset_aggregation` pins bounded day offset rounding, `date_histogram_fixed_minute_interval_aggregation`, `date_histogram_fixed_hour_interval_aggregation`, and `date_histogram_fixed_day_interval_aggregation` pin bounded `fixed_interval: 1m` / `1h` / `1d` bucket rounding, `date_histogram_calendar_minute_interval_aggregation`, `date_histogram_calendar_hour_interval_aggregation`, `date_histogram_calendar_week_interval_aggregation`, `date_histogram_calendar_month_interval_aggregation`, and `date_histogram_calendar_year_interval_aggregation` pin bounded calendar `minute` / `hour` / `week` / `month` / `year` bucket rounding, `date_histogram_extended_bounds_aggregation` pins bounded day `extended_bounds` empty bucket expansion, `date_histogram_hard_bounds_aggregation` pins bounded day `hard_bounds` bucket-key filtering, `date_histogram_min_doc_count_aggregation` pins bounded `min_doc_count: 1` empty-bucket suppression, `date_histogram_format_epoch_millis_aggregation`, `date_histogram_format_custom_aggregation`, and `date_histogram_format_basic_date_time_no_millis_aggregation` pin bounded `format` key string rendering, `date_histogram_time_zone_aggregation` pins bounded fixed-offset `time_zone` rounding/rendering, and `date_histogram_missing_aggregation` pins bounded string date missing replacement; IANA/DST time zones, broader format variants, and broader interval variants remain partial. |
| `histogram` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native collection in [os-engine-tantivy/lib.rs](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs); standalone fallback in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs). | `histogram_aggregation` pins bounded numeric interval buckets against OpenSearch, `histogram_keyed_aggregation` pins keyed object bucket output, `histogram_offset_aggregation` pins bounded numeric offset bucket rounding, `histogram_extended_bounds_aggregation` pins bounded numeric `extended_bounds` empty bucket expansion, `histogram_hard_bounds_aggregation` pins bounded numeric `hard_bounds` bucket-key filtering, `histogram_min_doc_count_aggregation` pins bounded `min_doc_count: 1` empty-bucket suppression, and `histogram_missing_aggregation` pins bounded numeric missing replacement; broader numeric coercion variants remain partial. |
| `terms` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native collection in [os-engine-tantivy/lib.rs](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs); standalone fallback in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs). | `terms_aggregation` pins bounded keyword buckets against OpenSearch, `terms_min_doc_count_aggregation` pins bounded `min_doc_count: 2` bucket suppression, `terms_include_exclude_aggregation` pins bounded array-form include/exclude filtering, `terms_regex_include_exclude_aggregation` pins bounded regex include/exclude filtering, `terms_partition_include_aggregation` pins bounded OpenSearch Murmur3 partition include filtering, `terms_missing_aggregation` pins bounded scalar missing replacement, `terms_size_sum_other_doc_count_aggregation` pins bounded `size` truncation plus `sum_other_doc_count`, `terms_keyed_rejected_aggregation` pins OpenSearch's `keyed` rejection, and `terms_key_order_aggregation` / `terms_order_aggregation` pin `_key` and `_count` ordering; shard-size accuracy and broader distributed ordering remain partial. |
| metric aggregations | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | aggregation builder at [standalone_runtime.rs:15117](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:15117). | `metric_aggregations` pins bounded `min`, `max`, `sum`, `avg`, and `value_count` over numeric fields against OpenSearch; `metric_weighted_avg_aggregation` pins bounded OpenSearch-form `weighted_avg`; `geo_centroid_aggregation` pins bounded `geo_centroid` response shape and values; `metric_aggregation_missing_option` now pins numeric metric `missing` replacement behavior for `min`, `max`, `sum`, `avg`, and `stats`; `metric_extended_stats_aggregation` pins bounded `extended_stats` response shape and numeric values; `metric_percentiles_aggregation` and `metric_percentile_ranks_aggregation` pin bounded percentile-family response shape and numeric values; `metric_median_absolute_deviation_aggregation` pins bounded `median_absolute_deviation` response shape and numeric value; broader metric families remain partial. |
| `range` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native collection in [os-engine-tantivy/lib.rs:39138](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs:39138); standalone fallback at [standalone_runtime.rs:36524](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:36524). | `range_aggregation` pins bounded keyed numeric range bucket counts against OpenSearch, `range_keyed_aggregation` pins `keyed: true` object bucket output, and `range_missing_aggregation` pins bounded numeric missing replacement; scripted value sources, nested aggregation, and broader numeric coercion variants remain partial. |
| `date_range` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native collection around [os-engine-tantivy/lib.rs:163675](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs:163675); standalone fallback at [standalone_runtime.rs:36374](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:36374). | `date_range_aggregation` pins bounded keyed `from`/`to` date bucket counts and string bounds against OpenSearch, `date_range_keyed_aggregation` pins `keyed: true` object bucket output, and `date_range_missing_aggregation` pins bounded missing date replacement; broader format/time-zone variants remain partial. |
| `geo_distance` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native collection around [os-engine-tantivy/lib.rs:164031](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs:164031); standalone fallback at [standalone_runtime.rs:36306](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:36306). | `geo_distance_aggregation` pins bounded keyed geo-point distance buckets with `km` unit against OpenSearch, `geo_distance_keyed_aggregation` pins `keyed: true` object bucket output, and `geo_distance_missing_aggregation` pins bounded missing geo-point replacement; distance type variants, scripted value sources, and edge precision cases remain partial. |
| `ip_range` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native collection around [os-engine-tantivy/lib.rs:163589](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs:163589); standalone fallback at [standalone_runtime.rs:36429](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:36429). | `ip_range_aggregation` pins bounded keyed IPv4 `from`/`to` range bucket counts against OpenSearch, `ip_range_mask_aggregation` pins bounded IPv4 CIDR `mask` range buckets, `ip_range_keyed_aggregation` pins `keyed: true` object bucket output, and `ip_range_missing_aggregation` pins bounded IPv4 missing replacement; IPv6-specific cases and scripted value sources remain partial. |
| `missing` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native collection in [os-engine-tantivy/lib.rs:37787](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs:37787); standalone fallback at [standalone_runtime.rs:36016](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:36016). | `missing_bucket_aggregation` pins bounded optional numeric-field missing bucket `doc_count` behavior against OpenSearch, and `missing_keyword_bucket_aggregation` pins all-present keyword-field missing bucket zero-count behavior; broader nested missing semantics remain partial. |
| `multi_terms` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native plugin collection around [os-engine-tantivy/lib.rs:161485](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs:161485); standalone fallback at [standalone_runtime.rs:35686](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:35686). | `multi_terms_aggregation` pins bounded keyword pair bucket keys and `_count` ordering against OpenSearch; broader per-term options, error bounds, and distributed ordering remain partial. |
| `rare_terms` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native collection in [os-engine-tantivy/lib.rs:163817](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs:163817); standalone fallback at [standalone_runtime.rs:35634](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:35634). | `rare_terms_aggregation` pins bounded keyword `max_doc_count=1` bucket behavior against OpenSearch, `rare_terms_include_exclude_aggregation` pins bounded array-form include/exclude filtering, `rare_terms_regex_include_exclude_aggregation` pins bounded regex include/exclude filtering, and `rare_terms_numeric_field_aggregation` pins bounded numeric-field buckets with numeric key ordering; precision and distributed accuracy behavior remain partial. |
| `sampler` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native plugin collection around [os-engine-tantivy/lib.rs:164889](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs:164889); standalone fallback at [standalone_runtime.rs:35501](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:35501). | `sampler_aggregation` pins bounded sampler wrapper `doc_count` plus nested `terms` behavior against OpenSearch with `shard_size` above the fixture document count; score-sensitive sampling order remains partial. |
| `top_hits` aggregation | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-compat.json`, `tools/fixtures/search-strict-compat.json`, `crates/os-engine-tantivy/src/lib.rs`, `crates/os-node/src/standalone_runtime.rs` | native collection in [os-engine-tantivy/lib.rs:37579](/home/ubuntu/steelsearch/crates/os-engine-tantivy/src/lib.rs:37579); standalone fallback at [standalone_runtime.rs:36016](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:36016). | `top_hits_aggregation`, `top_hits_sorted_aggregation`, `top_hits_source_filter_aggregation`, `top_hits_docvalue_fields_aggregation`, `top_hits_fields_aggregation`, and `top_hits_from_sorted_aggregation` pin bounded size, explicit sort, `_source` include/exclude filtering, bounded `docvalue_fields`, bounded fetch `fields`, and from-window behavior against OpenSearch; broader fetch sub-options remain partial. |
| filter aggregations | `/_search`, `/{index}/_search` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | aggregation builder at [standalone_runtime.rs:15117](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:15117). | `filter_aggregations` pins bounded single `filter` and keyed `filters` aggregations against OpenSearch; broader filter variants remain partial. |

## Template Search Families

| Family | Surface | Implemented | Strict compared | Semantic probed | Fail closed | Evidence | Code path / missing path | Notes / missing work |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| stored/named template render | `/_render/template`, `/_render/template/{id}` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | template resolution in `search_template_search_body` / related template helpers around [standalone_runtime.rs:5263](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5263). | `render_template_named_search` pins named stored-template rendering against OpenSearch; missing-param, malformed-source, and stored-script overwrite behavior remain covered semantically. |
| stored/named template search | `/_search/template`, `/{index}/_search/template` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | `handle_search_template_route` at [standalone_runtime.rs:4709](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:4709); template expansion at [standalone_runtime.rs:5263](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5263). | `search_template_named_target_search` pins named template substitution and targeted execution against OpenSearch; missing/extra-param and malformed-source cases remain semantic/fail-closed coverage. |
| multi-search template | `/_msearch/template`, `/{index}/_msearch/template` | partial | yes | yes | partial | `tools/fixtures/search-strict-compat.json`, `tools/fixtures/search-semantic-compat.json`, `target/phase-a-acceptance-harness/local/compare/search-compat-report.json` | `handle_msearch_template_route` at [standalone_runtime.rs:4746](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:4746). | `msearch_template_named_root_search` pins named template expansion for multi-request bodies against OpenSearch; malformed and broader per-item option coverage remains partial. |

## Vector / k-NN Families

| Family | Surface | Implemented | Strict compared | Semantic probed | Fail closed | Evidence | Code path / missing path | Notes / missing work |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `knn` basic query | `/_search`, `/{index}/_search` | partial | partial | yes | partial | `tools/fixtures/vector-search-compat.json`, `target/phase-a-acceptance-harness/local/compare/vector-search-compat-report.json`, `crates/os-node/src/standalone_runtime.rs` | target capability validation at [standalone_runtime.rs:12177](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12177); query scoring at [standalone_runtime.rs:13953](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:13953). | `vector-ml` compare pins top-level `knn`, `k`, filter, `ignore_unmapped`, radial `max_distance`, `method_parameters`, byte/binary vector subsets, nested filtered `knn`, and bounded hybrid placements with exact hit-order comparison. Broader profile-specific and distributed ranking work remains partial. |
| cosine / inner-product variants | `/_search`, `/{index}/_search` | partial | partial | yes | partial | `tools/fixtures/vector-search-compat.json`, `target/phase-a-acceptance-harness/local/compare/vector-search-compat-report.json` | vector scoring path at [standalone_runtime.rs:13953](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:13953) and [standalone_runtime.rs:15002](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:15002). | `knn_cosinesimil_search` and `knn_innerproduct_search` pass OpenSearch/Steelsearch comparison in the vector profile. |
| unsupported method / mode / parameters | `/_search`, `/{index}/_search` | partial | partial | yes | yes | `tools/fixtures/vector-search-compat.json`, `tools/fixtures/search-compat.json`, `target/phase-a-acceptance-harness/local/compare/vector-search-compat-report.json` | explicit negative validation in [standalone_runtime.rs:12177](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12177) and [standalone_runtime.rs:12747](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12747). | `knn_query_unsupported_parameter_error` and hybrid invalid-shape errors are pinned by vector compare; broader unsupported method/mode fixtures remain covered by the strict search fixture. |


## Ignored Or Partial Body-Field Inventory

This table tracks top-level search body fields that are either:

- explicitly consumed today;
- explicitly rejected today; or
- not yet backed by a dedicated handler/evaluator path and therefore require a
  fail-closed vs documented-partial decision.

| Field | Current status | Evidence | Notes / next action |
| --- | --- | --- | --- |
| `query` | consumed | `validate_search_query_body` at [standalone_runtime.rs:12699](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12699), evaluation via [standalone_runtime.rs:5667](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5667) | Primary supported body field. |
| `aggs` | consumed | `build_search_aggregations` at [standalone_runtime.rs:15117](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:15117) | Partial aggregation family support. |
| `sort` | consumed + validated | sort parsing, validation, hit ordering, and sort-value rendering in `standalone_runtime.rs` | Supports OpenSearch string shorthand plus array/object field sorts for the bounded standalone route, including multi-field sort objects expanded into ordered field-sort entries; malformed sort entries, invalid order strings, invalid `mode` strings, non-scalar `missing` values, invalid `unmapped_type`, unmapped field sorts without `unmapped_type`, and numeric-only modes on non-numeric fields fail closed; explicit `missing` `_first` / `_last`, default missing-last placement, scalar replacement values, `unmapped_type`, and multi-valued sort `mode` selection are applied during ordering. |
| `from` / `size` | consumed + validated | pagination and query-param precedence in [standalone_runtime.rs:5878](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5878) and `apply_search_source_query_params` | `sorted_paginated_search_window`, `query_param_paginated_search_window`, `negative_from_body_error_search`, `negative_size_body_error_search`, `negative_from_query_param_error_search`, and `negative_size_query_param_error_search` strict-compare body/query-param pagination, REST query-param precedence over body, and negative bound rejection. |
| `indices_boost` | consumed + validated | validation and index score multiplication in `standalone_runtime.rs` | Supports OpenSearch array format plus deprecated object format for resolved index names and wildcard selectors. |
| `terminate_after` | consumed + validated | hit truncation, total-hit shaping, query-param precedence, and `terminated_early` true/false rendering in `standalone_runtime.rs` | `terminate_after_search`, `terminate_after_query_param_search`, `terminate_after_not_reached_search`, and `terminate_after_zero_query_param_preserves_body_search` strict-compare body/query-param limits, reached/not-reached response shaping, and OpenSearch's zero query-param preserve-body behavior. |
| `track_total_hits` / `rest_total_hits_as_int` | consumed + validated | validation at [standalone_runtime.rs:12283](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12283), query-param precedence in `standalone_runtime.rs`, response shaping at [standalone_runtime.rs:5869](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5869) | `track_total_hits_threshold_search`, `track_total_hits_true_search`, `track_total_hits_query_param_threshold_overrides_body_search`, `track_total_hits_query_param_disabled_search`, `rest_total_hits_as_int_search`, `rest_total_hits_as_int_disabled_search`, and `rest_total_hits_as_int_inaccurate_error_search` strict-compare bounded body, query-param, threshold, disabled, int-total, and inaccurate-total rejection shaping. |
| `track_scores` | consumed + validated | body/query-param boolean parsing and field-sort `_score` / `max_score` response shaping in `standalone_runtime.rs` | Mirrors OpenSearch field-sort behavior for the bounded standalone route: scores are hidden unless `track_scores=true` or `_score` sorting is requested; `track_scores_field_sort_search`, `track_scores_body_field_sort_search`, and `track_scores_query_param_overrides_body_search` strict-compare query-param, body, and REST-param precedence behavior. |
| `highlight` | consumed + validated | validation at [standalone_runtime.rs:20986](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:20986), rendering at [standalone_runtime.rs:23248](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:23248) | Semantic compare covers encoder admission and custom-tag match highlighting; partial family support. |
| `suggest` | consumed + validated | validation at [standalone_runtime.rs:21056](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:21056), rendering at [standalone_runtime.rs:23411](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:23411) | Semantic compare covers bounded term, phrase, and completion suggesters; partial family support. |
| `pit` | consumed + validated | validation at [standalone_runtime.rs:12302](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12302), PIT resolution at [standalone_runtime.rs:5705](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5705), lifecycle handlers around [standalone_runtime.rs:9952](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:9952) | Strict compare now covers open, create-PIT `preference` / indices-options / `allow_partial_pit_creation` REST options, list, delete, delete-all, missing-context errors after close, snapshot stability after update/delete, PIT search parameter rejection, and PIT search keep-alive extension reflected in `_search/point_in_time/_all`; broader distributed resource accounting remains production-hardening work. |
| `search_after` | consumed + validated | validation at [standalone_runtime.rs:12307](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12307), application at [standalone_runtime.rs:13343](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:13343) | Partial family support. |
| `slice` | consumed + validated | validation and deterministic bounded hit filtering in `standalone_runtime.rs` | Rejected outside scroll/PIT context; bounded `_id`/source-field slicing is applied for supported search-session contexts. |
| `rescore` | consumed + validated | validation at [standalone_runtime.rs:12321](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12321), application at [standalone_runtime.rs:13448](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:13448) | Partial family support. |
| `collapse` | consumed + validated | validation at [standalone_runtime.rs:12331](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12331), application at [standalone_runtime.rs:13497](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:13497) | Partial family support. |
| `derived` | consumed + validated | validation and request-scoped source application in `standalone_runtime.rs` | Bounded OpenSearch derived-field passthrough support for direct `emit(...)` scripts. |
| `runtime_mappings` | consumed + validated | validation at [standalone_runtime.rs:12268](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:12268), application at [standalone_runtime.rs:5800](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs:5800) | `runtime_mappings_field_request_search` and `runtime_mappings_string_script_search` strict-compare bounded request-scoped runtime field query and field projection for `emit(doc[...].value)` and `emit(params._source[...])` scripts. |
| `stored_fields` | consumed + validated | validation, REST query-param admission, and OpenSearch `_none_` fetch-disable handling in `standalone_runtime.rs`; field extraction in `standalone_runtime.rs` | Bounded response-field path, including single-string input, empty selection default-source suppression, and `_none_` conflict checks. |
| `docvalue_fields` | consumed + validated | body and REST query-param validation plus field extraction in `standalone_runtime.rs` | Bounded response-field path; `docvalue_fields_epoch_millis_search` strict-compares date `format: epoch_millis`. |
| `explain` | consumed + validated | boolean body parsing plus REST query-param parsing in `standalone_runtime.rs`; response shaping emits bounded `_explanation` blocks. | `explain_search` and `explain_query_param_overrides_body_search` strict-compare body-enabled explain projection and REST-param precedence over body. |
| `profile` | consumed + validated | boolean body parsing in `standalone_runtime.rs`; bounded shaping emits a profile section only when requested. | `profile_search` and `profile_disabled_search` strict-compare true/false response presence; no dedicated profiling subsystem. |
| `_source` filtering | consumed + validated | bounded body and REST query-param source projection in `standalone_runtime.rs` | Include/exclude field selection supports simple fields, dot paths, array object paths, and `*` wildcard selectors; broader fetch-subphase integration remains partial. `source_filtering_wildcard_include_exclude_search` strict-compares object-form wildcard include plus explicit exclude behavior against OpenSearch, `source_filtering_query_param_include_exclude_search` pins the REST query-param include/exclude path, and `source_false_body_search` / `source_false_query_param_search` pin source suppression. |
| `fields` | consumed + validated | bounded fetch-field validation and hit `fields` projection in `standalone_runtime.rs` | Simple field extraction supports top-level fields, dot paths, source arrays, and date `format: epoch_millis`; broader field formatting semantics remain partial. |
| `script_fields` | consumed + validated | bounded script-field validation and hit `fields` projection in `standalone_runtime.rs` | `script_fields_projection_search` strict-compares simple `doc[...]` / `params._source[...]` field projection scripts; broader script execution remains fail-closed. |
| `post_filter` | consumed + validated | bounded query-shape validation and post-aggregation hit filtering in `standalone_runtime.rs` | Aggregation input is preserved for the bounded standalone path. |
| `timeout` / `cancel_after_time_interval` | consumed + validated | body and REST query-param time values are parsed/validated in `standalone_runtime.rs`; invalid values return OpenSearch-shaped illegal argument errors. | `timeout_search`, `cancel_after_time_interval_query_param_search`, and `cancel_after_time_interval_invalid_query_param_error_search` strict-compare accepted body/query timeout values and invalid query-param rejection; execution cancellation is not modeled yet. |
| `min_score` | consumed + validated | top-level numeric validation and hit score filtering in `standalone_runtime.rs`; k-NN query-level `min_score` remains separately validated. | Bounded score-gate support for the standalone route. |
| `version` | consumed + validated | boolean query/body parsing and hit `_version` projection in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs) | Bounded projection support; broad compare expansion still pending. |
| `seq_no_primary_term` | consumed + validated | boolean query/body parsing and hit `_seq_no`/`_primary_term` projection in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs) | Bounded projection support; broad compare expansion still pending. |
| `stats` | consumed + validated | body string-array validation and REST query-param comma splitting in [standalone_runtime.rs](/home/ubuntu/steelsearch/crates/os-node/src/standalone_runtime.rs) | `stats_body_groups_search` and `stats_query_param_groups_search` strict-compare body and URL stats groups as accepted observability metadata; no dedicated request stats aggregation is modeled. |


## Silent-Ignore Versus Fail-Closed Decision Table

This table records the current policy decision for top-level search fields that
are not yet backed by a dedicated handler/evaluator path.

| Field | Current behavior | Decision | Rationale | Follow-up |
| --- | --- | --- | --- | --- |
| `_source` filtering | bounded source projection | documented partial | Source retrieval now honors body/query include/exclude selection, dot paths, wildcard selectors, and `_source: false` while rejecting unsupported shapes; `source_filtering_search` strict-compares a bounded include projection, `source_filtering_object_array_path_search` pins object-form include/exclude projection through array object paths, `source_filtering_wildcard_include_exclude_search` pins wildcard include plus explicit exclude behavior, `source_filtering_query_param_include_exclude_search` pins REST query-param selectors, and `source_false_body_search` / `source_false_query_param_search` pin source suppression. | Expand strict compare fixtures for broader wildcard/object combinations. |
| `fields` | bounded source-backed field extraction | documented partial | Current support projects simple top-level, dot-path, and array values into hit `fields` and rejects unsupported shapes instead of silently ignoring them; `fields_top_level_search`, `fields_dot_path_array_search`, `fields_date_epoch_millis_search`, and `fields_unmapped_search` strict-compare bounded fetch-field projection, date formatting, and unmapped omission. | Expand strict compare fixtures for broader formats. |
| `post_filter` | bounded post-query hit filter phase | documented partial | The standalone path now filters hits after aggregation input is collected, matching the important result-shaping distinction for supported query families; `post_filter_search` strict-compares bounded term post-filter hit shaping, and `post_filter_preserves_aggregation_input_search` strict-compares preserved terms aggregation input. | Expand strict compare fixtures for broader query families. |
| `timeout` | bounded body/query-param parse validation | documented partial | Invalid time values no longer pass silently; successful timeout values are accepted while execution cancellation remains outside the bounded standalone model. | Add runtime cancellation semantics if this becomes operationally required. |
| top-level `min_score` | bounded top-level min-score gate | documented partial | The standalone path now rejects non-numeric values and excludes hits whose evaluated score is below the threshold; `min_score_search` strict-compares a bounded match-all threshold case. | Expand strict compare fixtures across scoring query families. |
| `version` | bounded search-hit version projection path | documented partial | Matching semantics are unchanged; requested hit metadata now renders for the standalone route; `hit_metadata_projection_search`, `hit_metadata_projection_body_search`, `hit_metadata_query_params_override_body_search`, `hit_metadata_query_params_enable_body_false_search`, `hit_metadata_version_query_param_overrides_body_only_search`, `hit_metadata_seq_query_param_overrides_body_only_search`, and `hit_metadata_invalid_version_query_param_error_search` strict-compare projection, REST-param precedence, and invalid REST boolean rejection. | Expand strict compare fixtures for broader hit-metadata combinations if needed. |
| `seq_no_primary_term` | bounded search-hit seq_no/primary_term projection path | documented partial | Matching semantics are unchanged; requested hit metadata now renders for the standalone route; `hit_metadata_projection_search`, `hit_metadata_projection_body_search`, `hit_metadata_query_params_override_body_search`, `hit_metadata_query_params_enable_body_false_search`, `hit_metadata_version_query_param_overrides_body_only_search`, `hit_metadata_seq_query_param_overrides_body_only_search`, and `hit_metadata_invalid_seq_no_primary_term_query_param_error_search` strict-compare projection, REST-param precedence, and invalid REST boolean rejection. | Expand strict compare fixtures for broader hit-metadata combinations if needed. |
| `stats` | body/query-param groups consumed and validated | documented partial | Stats groups are observability metadata; `stats_body_groups_search` and `stats_query_param_groups_search` now pin body and URL group admission against OpenSearch, while unsupported body shapes still fail closed instead of being silently ignored. | Add dedicated request stats aggregation if search usage accounting becomes an operational requirement. |

Decision rule used here:

- if silently ignoring the field can change matching, ranking, filtering,
  pagination, or timeout semantics, prefer `fail closed`;
- if the field is primarily about extra projection or observability metadata,
  `documented partial` is acceptable temporarily as long as the matrix and
  fixtures do not claim support.

## How To Read This Matrix

- `Implemented = partial` is expected for many families right now. It means the
  repository has live behavior and some evidence, but not enough to claim
  blanket OpenSearch replacement parity.
- `Strict compared = yes` is a stronger statement than `Semantic probed = yes`.
- `Fail closed = partial` means some unsupported variants already reject
  correctly, but the audit is not yet complete enough to assert that all
  unsupported variants reject.
- Rows should be promoted only when fixtures, runtime code, and evidence
  artifacts all align.
