# Generated Transport Action Reference

This file is generated from `docs/rust-port/generated/source-transport-actions.tsv`.
It is exhaustive at the transport-action inventory level.

Columns:

- `status`: current Steelsearch classification from the source compatibility matrix
- `action`: OpenSearch action identifier
- `transport_handler`: Java transport handler class registered for the action
- `opensearch_meaning`: semantic role of the action inside OpenSearch
- `steelsearch_behavior`: current Steelsearch implementation posture derived from status and docs
- `replacement_gap`: what still blocks parity

## root-cluster-node

| status | action | transport_handler | opensearch_meaning | steelsearch_behavior | replacement_gap | source | line |
| --- | --- | --- | --- | --- | --- | --- | --- |
| planned | `MainAction.INSTANCE` | `TransportMainAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 656 |
| planned | `NodesInfoAction.INSTANCE` | `TransportNodesInfoAction.class` | Node info transport action. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 657 |
| planned | `NodesStatsAction.INSTANCE` | `TransportNodesStatsAction.class` | Node statistics transport action. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 659 |
| planned | `WlmStatsAction.INSTANCE` | `TransportWlmStatsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 660 |
| planned | `NodesUsageAction.INSTANCE` | `TransportNodesUsageAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 663 |
| planned | `NodesHotThreadsAction.INSTANCE` | `TransportNodesHotThreadsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 664 |
| planned | `ListTasksAction.INSTANCE` | `TransportListTasksAction.class` | Transport action for task listing, lookup, completion, or cancellation. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 665 |
| planned | `GetTaskAction.INSTANCE` | `TransportGetTaskAction.class` | Transport action for task listing, lookup, completion, or cancellation. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 666 |
| planned | `CancelTasksAction.INSTANCE` | `TransportCancelTasksAction.class` | Transport action for task listing, lookup, completion, or cancellation. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 667 |
| planned | `AddVotingConfigExclusionsAction.INSTANCE` | `TransportAddVotingConfigExclusionsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 669 |
| planned | `ClearVotingConfigExclusionsAction.INSTANCE` | `TransportClearVotingConfigExclusionsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 670 |
| planned | `ClusterStatsAction.INSTANCE` | `TransportClusterStatsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 672 |
| planned | `ClusterStateAction.INSTANCE` | `TransportClusterStateAction.class` | Cluster-state transport action used to read authoritative cluster metadata. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 673 |
| planned | `ClusterHealthAction.INSTANCE` | `TransportClusterHealthAction.class` | Cluster-health transport action used by admin and health callers. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 675 |
| planned | `ClusterRerouteAction.INSTANCE` | `TransportClusterRerouteAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 677 |
| planned | `PendingClusterTasksAction.INSTANCE` | `TransportPendingClusterTasksAction.class` | Transport action for task listing, lookup, completion, or cancellation. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 679 |
| planned | `ClusterAddWeightedRoutingAction.INSTANCE` | `TransportAddWeightedRoutingAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 693 |
| planned | `ClusterGetWeightedRoutingAction.INSTANCE` | `TransportGetWeightedRoutingAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 694 |
| planned | `NodesReloadSecureSettingsAction.INSTANCE` | `TransportNodesReloadSecureSettingsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 765 |
| planned | `StartPersistentTaskAction.INSTANCE` | `StartPersistentTaskAction.TransportAction.class` | Transport action for task listing, lookup, completion, or cancellation. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 804 |
| planned | `CompletionPersistentTaskAction.INSTANCE` | `CompletionPersistentTaskAction.TransportAction.class` | Transport action for task listing, lookup, completion, or cancellation. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 806 |
| planned | `RemovePersistentTaskAction.INSTANCE` | `RemovePersistentTaskAction.TransportAction.class` | Transport action for task listing, lookup, completion, or cancellation. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 807 |

## index-and-metadata

| status | action | transport_handler | opensearch_meaning | steelsearch_behavior | replacement_gap | source | line |
| --- | --- | --- | --- | --- | --- | --- | --- |
| planned | `RemoteInfoAction.INSTANCE` | `TransportRemoteInfoAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 658 |
| planned | `GetTermVersionAction.INSTANCE` | `TransportGetTermVersionAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 674 |
| planned | `PruneFileCacheAction.INSTANCE` | `TransportPruneFileCacheAction.class` | Transport action used by vector-search or model-serving plugin flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 680 |
| planned | `GetRepositoriesAction.INSTANCE` | `TransportGetRepositoriesAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 682 |
| planned | `IndicesStatsAction.INSTANCE` | `TransportIndicesStatsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 696 |
| planned | `CatShardsAction.INSTANCE` | `TransportCatShardsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 697 |
| planned | `IndicesSegmentsAction.INSTANCE` | `TransportIndicesSegmentsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 698 |
| planned | `IndicesShardStoresAction.INSTANCE` | `TransportIndicesShardStoresAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 699 |
| planned | `ResizeAction.INSTANCE` | `TransportResizeAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 701 |
| planned | `RolloverAction.INSTANCE` | `TransportRolloverAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 702 |
| planned | `IndicesExistsAction.INSTANCE` | `TransportIndicesExistsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 707 |
| planned | `GetMappingsAction.INSTANCE` | `TransportGetMappingsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 709 |
| planned | `GetFieldMappingsAction.INSTANCE` | `TransportGetFieldMappingsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 710 |
| planned | `PutMappingAction.INSTANCE` | `TransportPutMappingAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 715 |
| planned | `AutoPutMappingAction.INSTANCE` | `TransportAutoPutMappingAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 716 |
| planned | `IndicesAliasesAction.INSTANCE` | `TransportIndicesAliasesAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 717 |
| planned | `AnalyzeAction.INSTANCE` | `TransportAnalyzeAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 720 |
| planned | `PutComponentTemplateAction.INSTANCE` | `TransportPutComponentTemplateAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 724 |
| planned | `GetComponentTemplateAction.INSTANCE` | `TransportGetComponentTemplateAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 725 |
| planned | `SimulateTemplateAction.INSTANCE` | `TransportSimulateTemplateAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 731 |
| planned | `ValidateQueryAction.INSTANCE` | `TransportValidateQueryAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 732 |
| planned | `FlushAction.INSTANCE` | `TransportFlushAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 734 |
| planned | `ForceMergeAction.INSTANCE` | `TransportForceMergeAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 735 |
| planned | `UpgradeAction.INSTANCE` | `TransportUpgradeAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 736 |
| planned | `UpgradeStatusAction.INSTANCE` | `TransportUpgradeStatusAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 737 |
| planned | `UpgradeSettingsAction.INSTANCE` | `TransportUpgradeSettingsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 738 |
| planned | `ClearIndicesCacheAction.INSTANCE` | `TransportClearIndicesCacheAction.class` | Transport action used by vector-search or model-serving plugin flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 739 |
| planned | `GetAliasesAction.INSTANCE` | `TransportGetAliasesAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 740 |
| planned | `GetSettingsAction.INSTANCE` | `TransportGetSettingsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 741 |
| planned | `GetAction.INSTANCE` | `TransportGetAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 744 |
| planned | `TermVectorsAction.INSTANCE` | `TransportTermVectorsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 745 |
| planned | `MultiTermVectorsAction.INSTANCE` | `TransportMultiTermVectorsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 746 |
| planned | `MultiGetAction.INSTANCE` | `TransportMultiGetAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 753 |
| planned | `ClearScrollAction.INSTANCE` | `TransportClearScrollAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 762 |
| planned | `RecoveryAction.INSTANCE` | `TransportRecoveryAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 763 |
| planned | `SegmentReplicationStatsAction.INSTANCE` | `TransportSegmentReplicationStatsAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 764 |
| planned | `AutoCreateAction.INSTANCE` | `AutoCreateAction.TransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 766 |
| planned | `PutStoredScriptAction.INSTANCE` | `TransportPutStoredScriptAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 769 |
| planned | `GetStoredScriptAction.INSTANCE` | `TransportGetStoredScriptAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 770 |
| planned | `GetScriptContextAction.INSTANCE` | `TransportGetScriptContextAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 772 |
| planned | `GetScriptLanguageAction.INSTANCE` | `TransportGetScriptLanguageAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 773 |
| planned | `FieldCapabilitiesAction.INSTANCE` | `TransportFieldCapabilitiesAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 775 |
| planned | `PutPipelineAction.INSTANCE` | `PutPipelineTransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 781 |
| planned | `GetPipelineAction.INSTANCE` | `GetPipelineTransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 782 |
| planned | `SimulatePipelineAction.INSTANCE` | `SimulatePipelineTransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 784 |
| planned | `CreateDataStreamAction.INSTANCE` | `CreateDataStreamAction.TransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 789 |
| planned | `GetDataStreamAction.INSTANCE` | `GetDataStreamAction.TransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 791 |
| planned | `DataStreamsStatsAction.INSTANCE` | `DataStreamsStatsAction.TransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 793 |
| planned | `CreateViewAction.INSTANCE` | `CreateViewAction.TransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 796 |
| planned | `GetViewAction.INSTANCE` | `GetViewAction.TransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 798 |
| planned | `ListViewNamesAction.INSTANCE` | `ListViewNamesAction.TransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 800 |
| planned | `RetentionLeaseActions.Add.INSTANCE` | `RetentionLeaseActions.Add.TransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 810 |
| planned | `RetentionLeaseActions.Renew.INSTANCE` | `RetentionLeaseActions.Renew.TransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 811 |
| planned | `RetentionLeaseActions.Remove.INSTANCE` | `RetentionLeaseActions.Remove.TransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 812 |
| planned | `ExtensionProxyAction.INSTANCE` | `ExtensionProxyTransportAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 831 |
| planned | `PauseIngestionAction.INSTANCE` | `TransportPauseIngestionAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 845 |
| planned | `ResumeIngestionAction.INSTANCE` | `TransportResumeIngestionAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 846 |
| planned | `GetIngestionStateAction.INSTANCE` | `TransportGetIngestionStateAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 847 |
| planned | `ListTieringStatusAction.INSTANCE` | `TransportListTieringStatusAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 852 |
| planned | `GetTieringStatusAction.INSTANCE` | `TransportGetTieringStatusAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 853 |

## document-and-bulk

| status | action | transport_handler | opensearch_meaning | steelsearch_behavior | replacement_gap | source | line |
| --- | --- | --- | --- | --- | --- | --- | --- |
| planned | `ClusterUpdateSettingsAction.INSTANCE` | `TransportClusterUpdateSettingsAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 676 |
| planned | `ClusterDeleteWeightedRoutingAction.INSTANCE` | `TransportDeleteWeightedRoutingAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 695 |
| planned | `CreateIndexAction.INSTANCE` | `TransportCreateIndexAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 700 |
| planned | `DeleteIndexAction.INSTANCE` | `TransportDeleteIndexAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 703 |
| planned | `GetIndexAction.INSTANCE` | `TransportGetIndexAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 704 |
| planned | `OpenIndexAction.INSTANCE` | `TransportOpenIndexAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 705 |
| planned | `CloseIndexAction.INSTANCE` | `TransportCloseIndexAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 706 |
| planned | `AddIndexBlockAction.INSTANCE` | `TransportAddIndexBlockAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 708 |
| planned | `UpdateSettingsAction.INSTANCE` | `TransportUpdateSettingsAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 718 |
| planned | `ScaleIndexAction.INSTANCE` | `TransportScaleIndexAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 719 |
| planned | `PutIndexTemplateAction.INSTANCE` | `TransportPutIndexTemplateAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 721 |
| planned | `GetIndexTemplatesAction.INSTANCE` | `TransportGetIndexTemplatesAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 722 |
| planned | `DeleteIndexTemplateAction.INSTANCE` | `TransportDeleteIndexTemplateAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 723 |
| planned | `DeleteComponentTemplateAction.INSTANCE` | `TransportDeleteComponentTemplateAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 726 |
| planned | `PutComposableIndexTemplateAction.INSTANCE` | `TransportPutComposableIndexTemplateAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 727 |
| planned | `GetComposableIndexTemplateAction.INSTANCE` | `TransportGetComposableIndexTemplateAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 728 |
| planned | `DeleteComposableIndexTemplateAction.INSTANCE` | `TransportDeleteComposableIndexTemplateAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 729 |
| planned | `SimulateIndexTemplateAction.INSTANCE` | `TransportSimulateIndexTemplateAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 730 |
| planned | `RefreshAction.INSTANCE` | `TransportRefreshAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 733 |
| planned | `IndexAction.INSTANCE` | `TransportIndexAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 743 |
| planned | `DeleteAction.INSTANCE` | `TransportDeleteAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 751 |
| planned | `UpdateAction.INSTANCE` | `TransportUpdateAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 752 |
| planned | `BulkAction.INSTANCE` | `TransportBulkAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 754 |
| planned | `DeleteStoredScriptAction.INSTANCE` | `TransportDeleteStoredScriptAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 771 |
| planned | `DeletePipelineAction.INSTANCE` | `DeletePipelineTransportAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 783 |
| planned | `DeleteDataStreamAction.INSTANCE` | `DeleteDataStreamAction.TransportAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 790 |
| planned | `ResolveIndexAction.INSTANCE` | `ResolveIndexAction.TransportAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 792 |
| planned | `DeleteViewAction.INSTANCE` | `DeleteViewAction.TransportAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 797 |
| planned | `UpdateViewAction.INSTANCE` | `UpdateViewAction.TransportAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 799 |
| planned | `UpdatePersistentTaskStatusAction.INSTANCE` | `UpdatePersistentTaskStatusAction.TransportAction.class` | Transport action for task listing, lookup, completion, or cancellation. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 805 |
| planned | `UpdateIngestionStateAction.INSTANCE` | `TransportUpdateIngestionStateAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 848 |

## search

| status | action | transport_handler | opensearch_meaning | steelsearch_behavior | replacement_gap | source | line |
| --- | --- | --- | --- | --- | --- | --- | --- |
| planned | `ClusterAllocationExplainAction.INSTANCE` | `TransportClusterAllocationExplainAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 671 |
| planned | `ClusterSearchShardsAction.INSTANCE` | `TransportClusterSearchShardsAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 678 |
| planned | `SearchAction.INSTANCE` | `TransportSearchAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 755 |
| planned | `StreamSearchAction.INSTANCE` | `StreamTransportSearchAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 757 |
| planned | `SearchScrollAction.INSTANCE` | `TransportSearchScrollAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 759 |
| planned | `MultiSearchAction.INSTANCE` | `TransportMultiSearchAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 760 |
| planned | `ExplainAction.INSTANCE` | `TransportExplainAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 761 |
| planned | `SearchViewAction.INSTANCE` | `SearchViewAction.TransportAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 801 |
| planned | `CreatePitAction.INSTANCE` | `TransportCreatePitAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 821 |
| planned | `DeletePitAction.INSTANCE` | `TransportDeletePitAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 822 |
| planned | `PitSegmentsAction.INSTANCE` | `TransportPitSegmentsAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 823 |
| planned | `GetAllPitsAction.INSTANCE` | `TransportGetAllPitsAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 824 |
| planned | `PutSearchPipelineAction.INSTANCE` | `PutSearchPipelineTransportAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 840 |
| planned | `GetSearchPipelineAction.INSTANCE` | `GetSearchPipelineTransportAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 841 |
| planned | `DeleteSearchPipelineAction.INSTANCE` | `DeleteSearchPipelineTransportAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 842 |

## vector-and-ml

| status | action | transport_handler | opensearch_meaning | steelsearch_behavior | replacement_gap | source | line |
| --- | --- | --- | --- | --- | --- | --- | --- |
| planned | `KNNStatsAction.INSTANCE` | `KNNStatsTransportAction.class` | Transport action used by vector-search or model-serving plugin flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `KNNPlugin.java` | 348 |
| planned | `KNNWarmupAction.INSTANCE` | `KNNWarmupTransportAction.class` | Transport action used by vector-search or model-serving plugin flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `KNNPlugin.java` | 349 |
| planned | `UpdateModelMetadataAction.INSTANCE` | `UpdateModelMetadataTransportAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `KNNPlugin.java` | 350 |
| planned | `TrainingJobRouteDecisionInfoAction.INSTANCE` | `TrainingJobRouteDecisionInfoTransportAction.class` | Transport action used by vector-search or model-serving plugin flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `KNNPlugin.java` | 351 |
| planned | `GetModelAction.INSTANCE` | `GetModelTransportAction.class` | Transport action used by vector-search or model-serving plugin flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `KNNPlugin.java` | 352 |
| planned | `DeleteModelAction.INSTANCE` | `DeleteModelTransportAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `KNNPlugin.java` | 353 |
| planned | `TrainingJobRouterAction.INSTANCE` | `TrainingJobRouterTransportAction.class` | Transport action used by vector-search or model-serving plugin flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `KNNPlugin.java` | 354 |
| planned | `TrainingModelAction.INSTANCE` | `TrainingModelTransportAction.class` | Transport action used by vector-search or model-serving plugin flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `KNNPlugin.java` | 355 |
| planned | `RemoveModelFromCacheAction.INSTANCE` | `RemoveModelFromCacheTransportAction.class` | Transport action used by vector-search or model-serving plugin flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `KNNPlugin.java` | 356 |
| planned | `SearchModelAction.INSTANCE` | `SearchModelTransportAction.class` | Transport action used by search or search-adjacent features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `KNNPlugin.java` | 357 |
| planned | `UpdateModelGraveyardAction.INSTANCE` | `UpdateModelGraveyardTransportAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `KNNPlugin.java` | 358 |
| planned | `ClearCacheAction.INSTANCE` | `ClearCacheTransportAction.class` | Transport action used by vector-search or model-serving plugin flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `KNNPlugin.java` | 359 |

## snapshot-migration-interop

| status | action | transport_handler | opensearch_meaning | steelsearch_behavior | replacement_gap | source | line |
| --- | --- | --- | --- | --- | --- | --- | --- |
| planned | `RemoteStoreStatsAction.INSTANCE` | `TransportRemoteStoreStatsAction.class` | Transport action used by repository, snapshot, remote-store, or restore flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 661 |
| planned | `RemoteStoreMetadataAction.INSTANCE` | `TransportRemoteStoreMetadataAction.class` | Transport action used by repository, snapshot, remote-store, or restore flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 662 |
| planned | `PutRepositoryAction.INSTANCE` | `TransportPutRepositoryAction.class` | Transport action used by repository, snapshot, remote-store, or restore flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 681 |
| planned | `DeleteRepositoryAction.INSTANCE` | `TransportDeleteRepositoryAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 683 |
| planned | `VerifyRepositoryAction.INSTANCE` | `TransportVerifyRepositoryAction.class` | Transport action used by repository, snapshot, remote-store, or restore flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 684 |
| planned | `CleanupRepositoryAction.INSTANCE` | `TransportCleanupRepositoryAction.class` | Transport action used by repository, snapshot, remote-store, or restore flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 685 |
| planned | `GetSnapshotsAction.INSTANCE` | `TransportGetSnapshotsAction.class` | Transport action used by repository, snapshot, remote-store, or restore flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 686 |
| planned | `DeleteSnapshotAction.INSTANCE` | `TransportDeleteSnapshotAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 687 |
| planned | `CreateSnapshotAction.INSTANCE` | `TransportCreateSnapshotAction.class` | Transport action used by repository, snapshot, remote-store, or restore flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 688 |
| planned | `CloneSnapshotAction.INSTANCE` | `TransportCloneSnapshotAction.class` | Transport action used by repository, snapshot, remote-store, or restore flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 689 |
| planned | `RestoreSnapshotAction.INSTANCE` | `TransportRestoreSnapshotAction.class` | Transport action used by repository, snapshot, remote-store, or restore flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 690 |
| planned | `SnapshotsStatusAction.INSTANCE` | `TransportSnapshotsStatusAction.class` | Transport action used by repository, snapshot, remote-store, or restore flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 691 |
| planned | `ListDanglingIndicesAction.INSTANCE` | `TransportListDanglingIndicesAction.class` | Transport action used by repository, snapshot, remote-store, or restore flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 815 |
| planned | `ImportDanglingIndexAction.INSTANCE` | `TransportImportDanglingIndexAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 816 |
| planned | `DeleteDanglingIndexAction.INSTANCE` | `TransportDeleteDanglingIndexAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 817 |
| planned | `FindDanglingIndexAction.INSTANCE` | `TransportFindDanglingIndexAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 818 |
| planned | `RestoreRemoteStoreAction.INSTANCE` | `TransportRestoreRemoteStoreAction.class` | Transport action used by repository, snapshot, remote-store, or restore flows. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 827 |
| planned | `DecommissionAction.INSTANCE` | `TransportDecommissionAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 835 |
| planned | `GetDecommissionStateAction.INSTANCE` | `TransportGetDecommissionStateAction.class` | OpenSearch transport action that still needs explicit compatibility treatment. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 836 |
| planned | `DeleteDecommissionStateAction.INSTANCE` | `TransportDeleteDecommissionStateAction.class` | Transport action used by write-path or document lifecycle features. | OpenSearch exposes this surface, but Steelsearch has not implemented it yet. | Steelsearch still needs Java-compatible request/response handling and fail-closed validation here. | `ActionModule.java` | 837 |
